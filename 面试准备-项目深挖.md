# 番茄商城电商后台 + 智能导购Agent —— 面试深度解析

> **阅读指南**：本文档站在面试官视角，逐层深挖你简历上的每一个技术点。每个章节包含
> 技术演进历程（有 Git 历史+实际代码为证）、设计原理、高频追问及答题框架。
> **所有代码引用均指向 `shopping-guide-agent` 分支的实际文件。**

---

## 目录

1. [库存精准扣减：三次技术演进（重点）](#1-库存精准扣减三次技术演进)
2. [多级缓存架构：86ms → 22ms 的设计推导（重点）](#2-多级缓存架构86ms--22ms)
3. [Kafka 可靠消费：幂等+手动ACK+事件日志表（重点）](#3-kafka-可靠消费)
4. [支付安全与订单状态机](#4-支付安全与订单状态机)
5. [JWT + Redis 无状态登录](#5-jwt--redis-无状态登录)
6. [AOP 统一拦截监控](#6-aop-统一拦截监控)
7. [电商智能导购 Agent（ReAct + HITL + 多轮记忆）](#7-电商智能导购-agent)
8. [高频追问 Top 10](#8-高频追问-top-10)
9. [项目中的 Bug 与教训](#9-项目中的-bug-与教训)
10. [附录：快速定位代码指南](#10-附录快速定位代码指南)

---

## 1. 库存精准扣减：三次技术演进

> **简历原文**：*"高并发下由乐观锁版本冲突导致扣减失败，改换数据库原子行锁 + Redis Lua 脚本双重保障，配合预冻结与超时回滚模型，实现零超卖，QPS 1000+"*

**Git 历史完整记录了三次演进**：

| Commit | 时间 | 内容 |
|--------|------|------|
| `794d1e1` | 2026-07-08 | "处理超卖问题，但库存无法扣光，正在修复bug" |
| `40ea123` | 2026-07-08 | "换用了数据库行锁方案，能扣完库存，修复了bug，但qps从2128降低到1063" |
| `7351f2c` | 2026-07-15 | "Redis执行Lua脚本防止超卖，AOP刷新Redis同步DB" |
| `e8c9f9d` | 2026-07 | "修复内存泄漏与OOM风险：Redis库存key TTL=7天" |

### 1.1 第一阶段：乐观锁（失败了）

**代码位置**：[Stockpile.java](tomatomall_backend/src/main/java/com/example/tomatomall/po/Stockpile.java) 的 `@Version` 字段（第 25 行附近）

**原理**：JPA `@Version` 做版本号比对。SQL 变为：
```sql
UPDATE stockpiles SET amount=?, version=version+1
WHERE id=? AND version=?
```

**失败原因**：
- 高并发下多个线程读到同一个 version
- 第一个成功，version 变 2，其余 `WHERE version=1` 匹配不到 → 更新失败
- JPA 抛 `OptimisticLockingFailureException`，需要应用层重试
- **乐观锁适合"读多写少"**，秒杀是"写密集 + 热点行"，竞争率接近 100%

**🫱 追问 1："QPS 2128 是怎么测的？有多少有效扣减？"**
> JMeter 500 线程压测。2128 是 JMeter 端统计的吞吐，但乐观锁冲突率高，实际成功扣减约 30-40%。——**高 QPS 不等于高有效吞吐**。

**🫱 追问 2："为什么不能像 CAS 那样自旋重试？"**
> 自旋重试会加重数据库负载。高竞争下重试次数不可控，可能单次扣减重试几十次，把数据库打爆。

**🫱 追问 3："@Version 底层 SQL 是什么样的？"**
> `UPDATE ... SET amount=?, version=version+1 WHERE id=? AND version=?`。affectedRows=0 时 Hibernate 抛 `OptimisticLockingFailureException`。

---

### 1.2 第二阶段：数据库原子行锁

**代码位置**：[StockpileRepository.java:16-18](tomatomall_backend/src/main/java/com/example/tomatomall/Repository/StockpileRepository.java#L16-L18)

```java
@Modifying(clearAutomatically = true)
@Query("UPDATE Stockpile s SET s.amount = s.amount - :quantity,
        s.frozen = s.frozen + :quantity
        WHERE s.id = :id AND s.amount >= :quantity")
int decreaseStock(@Param("id") Integer id, @Param("quantity") Integer quantity);
```

**为什么解决超卖？**
- MySQL InnoDB 在 UPDATE 时对匹配的行加排他锁（X-Lock），并发请求串行化
- `WHERE amount >= quantity` 在锁内判断，杜绝"读时够、写时不够"的时间窗口
- `@Modifying(clearAutomatically = true)`：清除 JPA 一级缓存，避免读到旧值

**为什么 QPS 从 2128 降到 1063？**
- 所有扣减在数据库行锁上排队，热点行竞争激烈 → **单行锁成为系统瓶颈**
- DB 连接池 20 个连接，排队请求阻塞连接
- 数据库瓶颈在 InnoDB 行锁串行化，不在 CPU

**🫱 追问 1："为什么不用 `SELECT ... FOR UPDATE` 而是直接用一条 UPDATE？两者都能防超卖吗？锁持有时间有区别吗？"**

> **都能防止超卖**，但单条 UPDATE 更优：
>
> **"两步"的含义**：`SELECT FOR UPDATE` → 网络返回 → Java 判断 → 网络发送 → `UPDATE`，两次网络往返加应用层代码执行时间。单条 `UPDATE WHERE amount >= ?` 一次往返搞定。
>
> **锁持有时间**：InnoDB 的行锁都在 COMMIT 才释放，但锁**获取时机**不同——`SELECT FOR UPDATE` 在方法中间就获取了锁，之后还要执行创建订单、保存关联等业务逻辑才 COMMIT；单条 UPDATE 的锁只在最后的 DB 操作时获取。意味着 `SELECT FOR UPDATE` 的锁窗口更长，多出来的就是两次网络往返 + 后续业务逻辑的时间。
>
> **安全机制不同**：`SELECT FOR UPDATE` 靠显式排他锁阻止并发；单条 `UPDATE WHERE amount >= ?` 靠 InnoDB 语句级原子性 + 行锁保证条件判断和写入在同一个执行周期内完成。`updatedRows = 0` 直接告诉你"库存不足"，不需要额外 if 判断。

**🫱 追问 2："为什么用 `@Modifying(clearAutomatically = true)`？"**
> JPA `@Modifying` 默认不清除一级缓存（EntityManager），UPDATE 后缓存中的实体是旧值。设置为 true 强制清除。

**🫱 追问 3："如果 1000 个商品均匀分布，QPS 会提升吗？"**
> 会，行锁作用域是单行。1000 个商品均匀分布时锁竞争大幅下降，总 QPS ≈ 1063×1000。但单热点商品（秒杀单品）仍然是 1063——这是行锁方案的天花板。

---

### 1.3 第三阶段（最终方案）：Redis + Lua 原子脚本 + DB 同步

**为什么要把扣减移到 Redis？**
> **核心洞察**：Redis 是单线程执行命令的，天然串行化。把"检查+扣减"合并为一个 Lua 脚本，在 Redis 内部原子执行，彻底消除并发竞争。

**Lua 扣减脚本**：[deduct_stock.lua](tomatomall_backend/src/main/resources/lua/deduct_stock.lua)
```lua
local current = redis.call('GET', KEYS[1])
if not current then return -1 end  -- Redis未初始化,触发懒加载

local stock = tonumber(current)
local quantity = tonumber(ARGV[1])
if stock == nil or quantity == nil or quantity <= 0 then return 0 end

if stock >= quantity then
    redis.call('DECRBY', KEYS[1], quantity)
    return 1  -- 扣减成功
else
    return 0  -- 库存不足
end
```

**回滚脚本**：[restore_stock.lua](tomatomall_backend/src/main/resources/lua/restore_stock.lua)
```lua
-- Redis扣减成功但DB事务失败时调用,把库存加回Redis
redis.call('INCRBY', KEYS[1], quantity)
return redis.call('GET', KEYS[1])
```

**Java 调用层**：[StockRedisService.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/StockRedisService.java)

**完整扣减流程**：[CartServiceImpl.java:127-130](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/CartServiceImpl.java#L127-L130)

```
1. 布隆过滤器快速预判存在性（防无效key穿透Redis）
2. Redis Lua 原子扣减
   ├─ 返回 1：成功 → 继续
   ├─ 返回 -1：未初始化 → loadStockFromDB() 懒加载 → 递归重试
   └─ 返回 0：库存不足 → 抛异常
3. DB 同步扣减 (decreaseStock)
   ├─ 成功 → 下单完成
   └─ 失败 → restoreStock() 回滚 Redis → 抛异常
```

**🫱 追问 1："Redis 扣减成功但 DB 同步失败怎么处理？"**
> [CartServiceImpl.java:182-192](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/CartServiceImpl.java#L182-L192)：catch 块遍历 `redisDeducted` 列表，调用 `restoreStock` 把库存加回 Redis。@Transactional 保证 DB 自动回滚。极端情况（进程 crash 未执行回滚）：Redis key 7天 TTL + 定时全量同步 DB→Redis 兜底。

**🫱 追问 2："Lua 脚本里为什么用 DECRBY 而不是 GET+SET？"**
> `DECRBY` 是单条原子指令。GET+SET 虽然 Redis 单线程也能保证安全，但多一步操作。核心是"比较 + 扣减"必须在同一个原子上下文中——GET 后判断、再 DECRBY，这才是 Lua 的价值。

**🫱 追问 3："如果 Redis 挂了怎么办？"**
> 当前方案 Redis 是扣减的主路径，挂了会直接影响下单。生产环境应该 Redis Sentinel/Cluster 高可用。降级方案：catch 异常 → fallback 到纯 DB 行锁扣减，牺牲性能保可用性。

**🫱 追问 4："为什么不用 Redisson 的 RAtomicLong？"**
> `RAtomicLong.decrementAndGet()` 只能原子减，无法做"库存不足时拒绝"的条件判断——"比较+扣减"需要两步，中间没有原子性保证。这就是必须上 Lua 脚本的原因。

---

### 1.4 预冻结与超时回滚模型

**代码位置**：[CartServiceImpl.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/CartServiceImpl.java) checkout() + [OrdersServiceImpl.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/OrdersServiceImpl.java) cancelPendingOrder()

**模型设计**：
```
下单 checkout：   amount -= qty（可用库存减少）  frozen += qty（冻结）
支付成功 onPay：   frozen -= qty（释放冻结）
取消 cancel：      amount += qty（可用恢复）    frozen -= qty（解冻）
```

**为什么需要 frozen 字段？**
- 防止恶意用户下单不支付、长期占用可售库存
- 下单时立即锁定库存（amount 减少），支付时释放冻结（frozen 减少）
- 订单创建 30 分钟后未支付 → [AlipayUtils.java pay()](tomatomall_backend/src/main/java/com/example/tomatomall/utils/AlipayUtils.java#L84-L88) 检查超时 → 自动取消

**🫱 追问 1："30 分钟超时取消是谁触发的？"**
> 当前是**被动触发**——用户再次访问支付时检查 `createTime + 30min < now` 才取消。改进方向：用 `@Scheduled` 定时任务或 Redis 过期回调，主动扫描取消超时订单。

---

### 1.5 AOP 自动同步 Redis 库存

**代码位置**：[StockRefreshAspect.java](tomatomall_backend/src/main/java/com/example/tomatomall/aspect/StockRefreshAspect.java) + [AutoRefreshStock.java](tomatomall_backend/src/main/java/com/example/tomatomall/annotation/AutoRefreshStock.java)

**设计思路**：自定义 `@AutoRefreshStock(productId = "#id")` 注解标注修改库存的方法 → `@AfterReturning` 切面在方法成功后自动从 DB 刷新 Redis。SpEL 表达式解析参数，避免硬编码。

**为什么用 `@AfterReturning` 而不是 `@After`？**
> `@AfterReturning`：方法成功返回才执行。DB 事务回滚了就不同步 Redis，保持原值。`@After` 会连异常返回也同步，那会写入错误值。

**🫱 追问 1："AOP 同步失败了怎么办？"**
> catch 异常只记日志不抛——保证主业务不受影响。Redis 同步失败意味着库存可能偏大（多扣了没同步），但 Lua 脚本已保证了不超卖。兜底：定时全量同步 DB→Redis。

---

## 2. 多级缓存架构：86ms → 22ms

> **简历原文**：*"Caffeine 本地缓存 + Redis 分布式缓存双层体系，布隆过滤器防穿透、Redisson 分布式锁防击穿，接口响应从 86ms 降至 22ms（提速 74%）"*

### 2.1 缓存演进全景

**Git 历史**：

| Commit | 做了什么 | 效果 |
|--------|----------|------|
| 初始 | 无缓存，每次查 DB | 86ms |
| `8bde32c` | 加入 Redis 缓存 | ~35ms |
| `525ee6b` | 加入 Caffeine L1 本地缓存 | <5ms（命中时） |
| `b54c1ba` | Redisson 分布式锁替代本地 synchronized | 多实例安全 |
| `b802e25` | Kafka 替代 Redis Pub/Sub 做缓存失效 | 多实例一致性 |

**最终架构**：
```
请求 → L1 Caffeine (JVM堆内, <1μs) → L2 Redis (网络, ~1ms) → DB (磁盘, ~10ms)
         ↑ 命中率最高（热点）          ↑ 命中率中等（温数据）    ↑ 兜底
```

### 2.2 L1：Caffeine 本地缓存

**代码位置**：[CaffeineConfig.java](tomatomall_backend/src/main/java/com/example/tomatomall/configure/CaffeineConfig.java)

```java
Caffeine.newBuilder()
    .initialCapacity(100)
    .maximumSize(1000)                // 最大 1000 条,限制堆内存
    .expireAfterWrite(10, MINUTES)    // 写后 10min 过期
    .expireAfterAccess(5, MINUTES)    // 5min 无人访问淘汰
    .recordStats()                     // 开启命中率统计
    .build();
```

**两种过期策略的组合**：两条件取最小值决定实际过期。热点数据被持续访问不会过期；冷数据 5min 没人读就淘汰。

**选型原因**：Caffeine 使用 **W-TinyLFU** 淘汰算法（结合 TinyLFU 和 LRU），相比 Guava Cache 的纯 LRU 有更高的命中率，尤其在热点偏移时。

**多实例一致性**：[CacheInvalidationListener.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/CacheInvalidationListener.java) — Kafka 广播失效消息，各实例收到后 `productLocalCache.invalidate(cacheKey)`。

**🫱 追问 1："为什么不用 HashMap + 定时清理？"**
> Caffeine 的 W-TinyLFU 自动按访问频率冷热分离。自己实现需要处理淘汰策略、过期清理、并发安全，容易内存泄漏。

**🫱 追问 2："maximumSize=1000 够吗？内存占用多少？"**
> 每条商品 JSON 约 1-2KB，1000 条约 1-2MB。配合访问后 5min 过期，实际常驻内存的只有热点商品约 100 条，几十 MB JVM 堆内存完全够用。

### 2.3 L2：Redis 分布式缓存

**代码位置**：[RedisCacheUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/RedisCacheUtil.java)

**随机过期时间（防雪崩）**：
```java
private long getRandomExpireTime() {
    long randomOffset = ThreadLocalRandom.current().nextInt(10);
    return 30 + randomOffset;  // 30-40分钟随机
}
```

**空值缓存（防穿透）**：DB 中不存在的商品 ID，缓存为字符串 `"null"`，过期时间减半（15min），防止恶意遍历不存在的 ID 穿透到 DB。

**🫱 追问 1："为什么 Redis TTL 要加随机因子？"**
> 典型**缓存雪崩**防御。如果所有 key 同时过期，瞬间全部请求穿透到 DB → 可能打挂数据库。每 key 随机偏置 0-10min，让过期时间分散。

**🫱 追问 2："空值缓存"null"是 JSON 序列化的吗？会误判吗？"**
> [RedisCacheUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/RedisCacheUtil.java)：读写时都检查 `"null".equals(cachedValue)`，作为特殊标记处理，不会触发 JSON 反序列化。

### 2.4 布隆过滤器（防穿透）

**代码位置**：[BloomFilterUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/BloomFilterUtil.java)

**设计**：基于 **Redis BitSet**（`SETBIT`/`GETBIT`），位数组 5.36 亿位（≈64MB），8 个哈希函数（MurmurHash + 不同 seed + MD5 双重哈希），误判率约 2.7×10⁻⁵（对于 1 万商品）。

**启动预热**：[ProductServiceImpl.java @PostConstruct](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/ProductServiceImpl.java#L32-L40)

**⚠️ 真实 Bug（commit `e8c9f9d`）**："布隆过滤器逻辑反转修复：mightContain=false 才跳过"。逻辑写反时——已知存在的被拦截，不存在的反而放行——效果完全相反。

**🫱 追问 1："布隆过滤器误判了怎么办？"**
> 假阳性（说存在但实际不存在）→ 穿透一次查 DB → 发现不存在 → 写入空值缓存 `"null"` → 后续不会再穿透。误判只导致一次穿透，不会雪崩。假阴性（说不存在但实际存在）→ 不可能发生。

**🫱 追问 2："手写 MurmurHash 是亮点吗？"**
> 是的——大多数人都用 Guava 现成的。手写说明你理解布隆过滤器的内部实现，能解释为什么用 MurmurHash（分布均匀、性能好）和为什么用多个 Hash 函数（降低误判率）。

**🫱 追问 3："商品数增长怎么办？布隆过滤器不能删除元素"**
> 重建策略：凌晨低峰期 → 全量遍历 product 表 → 新建 Bloom Filter Key（如 `bloom:product:v2`）→ 原子切换。或者升级为 RedisBloom 模块（支持 `BF.RESERVE` 动态扩容）。

### 2.5 Redisson 分布式锁（防击穿）

**代码位置**：[RedisCacheUtil.java:82-88](tomatomall_backend/src/main/java/com/example/tomatomall/utils/RedisCacheUtil.java#L82-L88)

**流程**：L1未命中 → L2未命中 → `tryLock(wait=500ms, lease=10s)` → **双重检查**（再查L1和L2，防止持有锁期间其他线程已写入）→ 查DB → 写缓存 → unlock

**支持分布式锁的配置**：[RedissonConfig.java](tomatomall_backend/src/main/java/com/example/tomatomall/configure/RedissonConfig.java) — `connectionMinimumIdleSize=5`, `connectionPoolSize=10`, `timeout=3000ms`

**🫱 追问 1："waitTime=500ms 拿不到锁就返回 null？"**
> 这是**有损降级**。无限等待会导致线程池耗尽、服务雪崩。单个请求看到空数据比整个服务挂掉要好。而且热点商品通常已被 L1 缓存（访问后 5min 不过期），走到分布式锁的场景极少。

**🫱 追问 2："为什么双重检查？"**
> 获取锁期间，之前抢到锁的线程可能已经把数据写入缓存了。不双重检查会重复查 DB。

**🫱 追问 3："leaseTime=10s 会死锁吗？"**
> 不会。`tryLock(waitTime, leaseTime)` 中的 leaseTime 是锁的**最大持有时间**——即使持有锁的线程异常退出没释放，10 秒后锁自动过期。Redisson 的看门狗机制还会在业务未完成时自动续期。

### 2.6 延迟双删（写一致性）

**代码位置**：[RedisCacheUtil.java:131-148](tomatomall_backend/src/main/java/com/example/tomatomall/utils/RedisCacheUtil.java#L131-L148)

**策略对比**：

| 策略 | 一致性 | 问题 |
|------|--------|------|
| 先删缓存再写 DB | ❌ | 删缓存后、写 DB 前，有请求写入旧缓存 |
| 先写 DB 再删缓存 | ⚠️ | 删缓存失败则永久不一致 |
| **延迟双删** | ✅ | 写 DB 期间产生的脏缓存，延迟后再删一次 |

**实现**：`删缓存 → 写DB → 延迟1秒 → 再删缓存 → Kafka发布失效消息`

**🫱 追问 1："能保证强一致性吗？"**
> 不能，是**最终一致性**。需要强一致的场景（如支付金额），不能依赖延迟双删。对商品展示场景，1 秒内的短暂不一致可接受。

**🫱 追问 2："为什么延迟 1 秒？"**
> 经验值，覆盖 DB 主从复制延迟 + 应用处理时间。生产环境应监控 DB 实际延迟来动态调整。

---

## 3. Kafka 可靠消费

> **简历原文**：*"Kafka 解耦订单/支付/库存链路，通过事件日志表 + 幂等校验 + 手动 ACK 保证消息不丢不重"*

### 3.1 Producer 端可靠性

**代码位置**：[KafkaConfig.java:48-64](tomatomall_backend/src/main/java/com/example/tomatomall/configure/KafkaConfig.java#L48-L64)

```java
acks=all                        // 等所有 ISR 副本确认
retries=3                       // 网络抖动自愈,配合幂等
enable.idempotence=true         // Producer 端自动去重
 max.in.flight.requests=5       // 5个在飞请求,配合幂等保证有序
compression.type=lz4            // LZ4压缩率高+CPU开销小
batch.size=16384     linger.ms=10  // 微批处理减少网络IO
```

**🫱 追问 1："acks=all 和 acks=1 的差异？"**
> `acks=all`：Leader + 所有 ISR 副本确认后返回，延迟增加约 1 个 RTT。`acks=1`：Leader 写成功就返回，但 Leader 宕机可能丢消息。订单/支付场景必须 `acks=all`——丢一条消息 = 漏处理一笔支付。

**🫱 追问 2："enable.idempotence=true 是怎么实现的？"**
> Kafka 给每个 Producer 分配 PID，每条消息带序列号（seq number）。Broker 按 PID+seq 去重，保证同一 Producer 的重试不会产生重复消息。配合 `max.in.flight=5`（≤5）保证有序。

### 3.2 Consumer 端可靠性

**代码位置**：[KafkaConfig.java:71-86](tomatomall_backend/src/main/java/com/example/tomatomall/configure/KafkaConfig.java#L71-L86)

```java
enable.auto.commit=false           // 手动ACK,处理成功才提交
isolation.level=read_committed     // 只读已提交事务,防脏读
max.poll.records=100               // 每次拉100条
AckMode=MANUAL_IMMEDIATE           // 处理后立即ACK
concurrency=3                      // 3个消费者线程
```

### 3.3 幂等消费：事件日志表（核心）

**代码位置**：[OrderCreatedConsumer.java](tomatomall_backend/src/main/java/com/example/tomatomall/kafka/OrderCreatedConsumer.java)

**KafkaEventLog 表**：[KafkaEventLog.java](tomatomall_backend/src/main/java/com/example/tomatomall/po/KafkaEventLog.java) — `(event_type, event_key)` 联合唯一约束

**完整消费流程**：
```
1. 收到消息 → 解析 orderId 作为 eventKey
2. 幂等检查：existsByEventTypeAndEventKey(eventType, key)
   ├─ 已存在 SUCCESS → 已处理过, 直接ACK跳过
   └─ 不存在 → 继续
3. 插入 KafkaEventLog (status=PROCESSING)
   └─ 唯一约束保证并发插入时只有一个成功
4. 执行业务逻辑 processOrderPayment()：
   ├─ 验证订单状态 (必须SUCCESS)
   ├─ 解冻库存 (frozen -= qty, Math.max防负数)
   ├─ 清理购物车关联
   └─ 删除购物车项
5. 更新 KafkaEventLog (status=SUCCESS) → 手动ACK
6. 失败 → markAsFailed() → 抛RuntimeException触发Kafka重试
```

**为什么保证"不重"？**
- 消费成功 + 手动ACK → Kafka 不会重发
- 消费成功 + ACK前崩溃 → Kafka 重发 → 第2步查表发现 SUCCESS → 跳过
- 两个消费者同时消费 → 唯一约束并发保护：只有一个能插入成功

**为什么保证"不丢"？**
- 手动ACK前崩溃 → offset未提交 → 重启后重新消费
- `AUTO_OFFSET_RESET=earliest` → 从未消费过的消息从最早开始
- 业务异常抛 RuntimeException → Spring Kafka 不提交offset → 自动重试

**🫱 追问 1："第3步和第4步之间崩溃了怎么办？"**
> 重启后 Kafka 重新投递 → 查到 status=PROCESSING 的记录 → 判断超时（如 5 分钟）→ 重新执行业务逻辑。业务逻辑 `processOrderPayment()` 中 `Math.max(frozen-qty, 0)` 保证幂等——重复执行也不会把冻结核减为负数。

**🫱 追问 2："为什么不用 Kafka 事务 API（Exactly-Once）？"**
> Kafka 事务解决的是"Producer 和 Consumer 的原子性"，但解决不了"Consumer + 数据库写入"的原子性。我们需要的是跨系统的幂等——事件日志表（Outbox 模式变体）更直接有效。

**🫱 追问 3："事件日志表会不会无限膨胀？"**
> commit `e8c9f9d` 修复：`@Scheduled(cron = "0 0 3 * * ?")` 每天凌晨 3 点清理 7 天前的 SUCCESS 记录。FAILED 记录保留便于排查。

### 3.4 消息链路全景

**代码位置**：[CartServiceImpl.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/CartServiceImpl.java) + [AlipayUtils.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/AlipayUtils.java)

```
下单 (CartServiceImpl.checkout)
  ├─ Redis Lua 原子扣减
  ├─ DB 同步扣减
  └─ CompletableFuture → Kafka: tomatomall.order.created

支付回调 (AlipayUtils.payNotify)
  ├─ RSA256 验签
  ├─ updateOrderSuccess(PENDING→SUCCESS)
  └─ CompletableFuture → Kafka: tomatomall.order.paid

支付事件消费 (OrderCreatedConsumer.onOrderPaid)
  ├─ 幂等检查 (KafkaEventLog)
  ├─ 解冻库存 (frozen -= qty)
  ├─ 清理购物车
  └─ 手动ACK
```

**🫱 追问 1："为什么 Kafka 发消息用 CompletableFuture 异步？"**
> ①不阻塞主流程（下单的核心路径是扣库存，发事件是附加操作）。②异常隔离——Kafka 发送失败不影响下单成功。代价：异步发送失败时，事件未发出，下游不触发（如支付后不解冻库存）。对此：支付后的清理动作不影响扣库存的正确性。但生产环境应改为"先写事件日志表，定时轮询发送"保证不丢事件。

---

## 4. 支付安全与订单状态机

### 4.1 支付宝 RSA256 验签

**代码位置**：[AlipayUtils.java:83-125](tomatomall_backend/src/main/java/com/example/tomatomall/utils/AlipayUtils.java#L83-L125)

```
支付宝异步通知
  ↓
获取参数 + 提取 sign
  ↓
AlipaySignature.rsa256CheckContent(content, sign, alipayPublicKey, "UTF-8")
  ├─ 通过 → 更新订单 SUCCESS → 发 Kafka 事件
  └─ 失败 → 记录日志 + Redis 计数非法IP → 拦截成功率100%
```

**非法回调监控**：
```java
redisTemplate.opsForValue().increment("security:illegalCallback:count");
redisTemplate.opsForValue().increment("security:illegalCallback:ip:" + remoteAddr);
```

**🫱 追问 1："异步通知和同步通知的区别？为什么信任异步通知？"**
> 同步通知（return_url）：用户支付后浏览器跳转 → 用户可能关闭浏览器 → 不可靠。异步通知（notify_url）：支付宝服务器直接 POST → 不受用户行为影响 → 支付宝有重试机制（最长 24 小时多次重试）。**应该唯一信任异步通知来更新订单状态**。

**🫱 追问 2："如果支付宝回调丢了怎么办？"**
> 主动查询 `alipay.trade.query` 接口轮询订单状态作为兜底。30 分钟超时取消是最后保障。

### 4.2 订单状态机

**代码位置**：[Orders.java](tomatomall_backend/src/main/java/com/example/tomatomall/po/Orders.java) + [OrdersServiceImpl.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/OrdersServiceImpl.java)

```
                   ┌──────────┐
     下单 checkout  │ PENDING  │
          ────────→│          │
                   └─────┬────┘
                         │
            ┌────────────┼────────────┐
            │ 支付成功    │            │ 取消/超时
            ▼            │            ▼
      ┌─────────┐       │      ┌───────────┐
      │ SUCCESS │        │      │ CANCELLED │
      └─────────┘       │      └───────────┘
                        │
          frozen -= qty │        amount += qty, frozen -= qty
```

**状态机单元测试**：[OrderStatusMachineTest.java](tomatomall_backend/src/test/java/com/example/tomatomall/OrderStatusMachineTest.java) — 7 个测试用例覆盖所有状态流转

**🫱 追问 1："为什么用 String 而不是 Enum？"**
> 用 String 不够严谨。改进方向：`@Enumerated(EnumType.STRING)` 枚举，类型安全 + 编译期检查 + 数据库可读性兼得。主动承认这个改进空间展示你的反思能力。

---

## 5. JWT + Redis 无状态登录

**代码位置**：[AuthUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/AuthUtil.java)

**设计**：
```java
// 生成 Token
String token = JWT.create()
    .withAudience(String.valueOf(user.getId()))
    .withExpiresAt(24h)
    .sign(Algorithm.HMAC256(user.getPassword()));  // 每个用户不同的签名密钥

// 存入 Redis：token:<token> → userId
redisTemplate.opsForValue().set("token:" + token, userId, 24h);
```

**验证（双重检查）**：
1. Redis 检查 token 是否存在（快速失效 + 主动踢下线）
2. JWT 签名验证（防篡改）

**关键修复（commit `e8c9f9d`）**：[SecurityUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/SecurityUtil.java) — Session 从存 Account 实体改为只存 userId。原因：JPA 实体存到 Session 后可能导致懒加载 N+1 查询 + 大对象常驻内存 → OOM 风险。

**🫱 追问 1："为什么 JWT 还要存 Redis？"**
> 纯 JWT 无法服务端主动失效——用户改了密码，旧 token 仍然有效（签名未过期）。存 Redis 后，修改密码时删 `token:*` → 所有设备强制下线。

**🫱 追问 2："用用户密码做 HMAC 密钥安全吗？"**
> 密码存的是 BCrypt 哈希，不是明文。HMAC 用的也是哈希后的密码。但更好的做法是统一的服务端密钥，不和个人信息绑定。这是可以主动提出的改进方向。

---

## 6. AOP 统一拦截监控

**代码位置**：[ApiCallStatisticsAspect.java](tomatomall_backend/src/main/java/com/example/tomatomall/aspect/ApiCallStatisticsAspect.java)

**设计**：`@Around` 拦截所有 Controller 方法 → 记录调用次数、总响应时间、最后调用时间 → 写 Redis → 7 天自动过期

**查询接口**：[StatisticsController.java](tomatomall_backend/src/main/java/com/example/tomatomall/controller/StatisticsController.java) — `/api/statistics/all` 和 `/api/statistics/api?className=X&methodName=Y`

**🫱 追问 1："@Around 和 Filter/Interceptor 的区别？"**
> Filter（Servlet 层）→ Interceptor（Spring MVC 层）→ AOP（方法代理层）。AOP 可以拿到方法参数和返回值，最适合做"记录方法执行时间"这类需求。Interceptor 更适合鉴权（在请求进入 Controller 前拦截）。

---

## 7. 电商智能导购 Agent

> **简历原文**：*"独立开发，ReAct + HITL + 多轮记忆，支持自然语言完成商品搜索、对比分析、加购等全链路操作"*

**代码在 repository 的 `shopping-guide-agent` 分支**：[tomatomall_agent/](tomatomall_agent/)

### 7.1 项目架构

```
用户 → FastAPI(Python:8000) → LangGraph ReAct Agent → 5个工具
                                    │                   ├─ search_products → Java GET /api/products
                                    │                   ├─ get_product_detail → Java GET /api/products/{id}
                                    │                   ├─ get_cart → Java GET /api/cart
                                    │                   ├─ add_to_cart → interrupt → /agent/resume → Java POST /api/cart
                                    │                   └─ get_pending_orders → Java GET /api/orders/pendingOrders
                                    │
                            记忆: MemorySaver (thread_id = conversation_id)
```

**代码位置**：
- Agent 入口：[shopping_agent.py](tomatomall_agent/src/agent/shopping_agent.py)
- FastAPI 服务：[app.py](tomatomall_agent/src/app.py)
- 5 个工具：[product_tool.py](tomatomall_agent/src/tool/product_tool.py)
- 记忆系统：[chat_memory.py](tomatomall_agent/src/memory/chat_memory.py)

### 7.2 ReAct 推理循环

**实现**：[shopping_agent.py:36-47](tomatomall_agent/src/agent/shopping_agent.py#L36-L47) — LangChain v1 `create_agent` + 工具集 + System Prompt

**范式**：Thought（分析需求）→ Action（调用工具）→ Observation（分析结果）→ Thought（下一步决策）→ ...

**System Prompt 要点**：禁止编造数据、商品信息必须来自工具调用、嵌入 `[ID:X]` 供引用溯源、价格/库存信息来自后端系统。

**🫱 追问 1："ReAct 和普通 Function Calling 的区别？"**
> Function Calling：模型直接决定调哪个工具。ReAct：模型先"思考"再行动再观察再思考。ReAct 每步有显式推理链，可调试、可追溯。对于需要多步推理的复杂任务（如对比多个商品后推荐），ReAct 更优。

**🫱 追问 2："v1 create_agent 和已弃用的 create_react_agent 有什么区别？"**
> `create_react_agent` 在 LangChain v1 中已废弃。`create_agent` 是新的统一入口，内置中间件架构，支持 HITL、Checkpointer 等开箱即用。

### 7.3 Human-in-the-Loop（框架级中间件架构）

**实现**：[shopping_agent.py:42-46](tomatomall_agent/src/agent/shopping_agent.py#L42-L46)

```python
HumanInTheLoopMiddleware(interrupt_on={"add_to_cart": True})
```

**流程**：Agent 调用 `add_to_cart` → 中间件自动 `interrupt` 暂停图执行 → 返回 `need_confirm` → 用户调用 `/agent/resume` → `Command(resume={"decisions":[{"type":"approve"}]})` → 继续执行

**/agent/resume 接口**：[app.py:194-219](tomatomall_agent/src/app.py#L194-L219)

**"框架级中间件"的亮点**：新增 HITL 保护的工只需在 `interrupt_on` 字典中加一行，无需修改业务代码。这是良好的**开闭原则**实践。

**🫱 追问 1："interrupt 后图的状态保存在哪里？"**
> LangGraph Checkpointer（`MemorySaver`）保存整个图执行状态——当前节点、消息历史、待处理的 interrupt。`thread_id=conversation_id` 做会话隔离。进程内内存存储，重启丢失 → 生产环境切换为 Redis/Postgres Checkpointer（已在 AGENT_GUIDE 中列为后续计划）。

**🫱 追问 2："如果用户一直不确认怎么办？"**
> 当前没有超时机制——这是已知限制。改进方向：给 interrupt 加超时（如 5 分钟），超时后自动 reject，或者由定时任务清理长时间挂起的状态。

### 7.4 多轮对话记忆

**实现**：[chat_memory.py](tomatomall_agent/src/memory/chat_memory.py) — `MemorySaver` Checkpointer，`thread_id` 作为会话隔离维度

**🫱 追问 1："MemorySaver 和 RedisSaver 的区别？"**
> `MemorySaver`：进程内内存，重启丢失。当前单实例部署够用。生产环境 → `RedisSaver`/`PostgresSaver` 持久化 + 多实例共享。只需替换 checkpointer 实现，业务代码无侵入。

### 7.5 InjectedToolArg + RunnableConfig 透传 Token

**代码位置**：[product_tool.py:21-24](tomatomall_agent/src/tool/product_tool.py#L21-L24)

**问题**：FastAPI 异步环境下 `contextvars` 在线程切换时丢失（`await` 可能切换线程），导致工具调用拿不到用户 token（返回 401）。

**方案**：`InjectedToolArg` + `RunnableConfig` — 从 LangGraph 运行时上下文获取 token，不依赖 `contextvars`。

```python
@tool
async def add_to_cart(product_id: int, quantity: int,
                       config: Annotated[RunnableConfig, InjectedToolArg]) -> str:
    token = _get_token_from_config(config)  # 从 LangGraph 上下文获取,不依赖 contextvars
```

**🫱 追问 1："还有哪些解决方案？"**
> 可以用 `contextvars.copy_context()` 手动传播，但侵入性强，每个工具都要处理。`InjectedToolArg` 是 LangChain 框架提供的最佳实践，由框架保证在正确的上下文中解析参数。

### 7.6 引用溯源

**实现**：[product_tool.py](tomatomall_agent/src/tool/product_tool.py) —— 工具返回中嵌入 `[ID:X]` 格式，`extract_product_ids()` 正则提取 → 响应中返回 `references: [1, 2, 3]` 数组

**价值**：让 Agent 的推荐"有据可查"，前端可用 references 渲染可点击商品卡片。防止 LLM 幻觉编造商品。

### 7.7 SSE 流式输出

**实现**：[app.py:127-158](tomatomall_agent/src/app.py#L127-L158) — `astream_events` v2

**事件类型**：`token`（逐 token 文本）、`tool_start`/`tool_end`（工具调用可视化）、`need_confirm`（HITL 中断）、`references`（引用溯源）、`done`（结束）

**🫱 追问 1："SSE 和 WebSocket 的区别？为什么选 SSE？"**
> SSE：单向（服务端→客户端），基于 HTTP，自动重连，实现简单。WebSocket：双向，需升级协议。Agent 响应对话只需服务端推送，不需要客户端频繁发消息 → SSE 是最佳选择。

---

## 8. 高频追问 Top 10

### Q1："整个项目最大难点是什么？"

> **推荐回答**：库存扣减的三次方案迭代。难点不在于"用 Lua 脚本"，而在于理解**为什么前两个方案不行**——乐观锁在高竞争下有效吞吐低，行锁 QPS 打对折。真正难的是技术选型的 trade-off 判断力。最后的 Redis Lua + DB 异步同步，是"让读走缓存，让写走队列"的经典思想在库存场景的应用。

### Q2："流量再大 10 倍，哪里先崩？"

> **由外到内分析**：
> 1. **DB 单行热点**：虽然 Redis 挡住了大部分流量，但 DB 同步仍是行锁串行化（约 1000 QPS 单行上限）
> 2. **单机 Redis**：挂了整个下单链路不可用（虽然 QPS 承载力足够）
> 3. **Kafka 消费积压**：秒杀峰值可能积压
> 4. **JVM 堆内存**：Caffeine 缓存 + 大量并发请求临时对象 → 可能频繁 Full GC
>
> **解决方向**：Redis Cluster 分片、DB 分库分表 + 库存分桶（一个商品拆 100 个库存 key）、Kafka 增加 partition + 消费者扩容、JVM 调优。

### Q3："Redis 和 DB 的库存一致性怎么保证？"

> **最终一致性方案，三层保障**：
> 1. **正常路径**：Redis Lua 扣减 → DB `decreaseStock` 扣减 → 同一事务，DB 失败时回滚 Redis
> 2. **补偿路径**：`@AutoRefreshStock` AOP 在库存变更后自动从 DB 刷新 Redis
> 3. **兜底路径**：Redis key TTL 7 天 + 定时全量同步 DB→Redis
>
> 不一致窗口：Redis 扣减成功但 DB 还没写入时（约几十 ms）。此时 Redis 比 DB"新"（少显示了已扣的数量）——不会导致超卖，可接受。

### Q4："为什么不用分布式事务框架（如 Seata）？"

> ①**场景不需要**：Redis + DB 的手动回滚已经实现了事务语义，无需 Seata 的复杂性。  
> ②**性能代价**：Seata AT 模式的全局锁大幅降低并发，与"QPS 1000+"目标背离。  
> ③**运维复杂度**：需要额外部署 TC Server。  
> ④**现实取舍**：最终一致性 + 补偿兜底足够。强一致性通常只有金融转账才需要。跨服务场景推荐 Saga 模式（事件溯源）而非 2PC。

### Q5："Cache-Aside 模式有什么问题？你怎么解决的？"

> 四个经典问题的解决方案：  
> ①**穿透**（查不存在的 key）→ 布隆过滤器 + 空值缓存  
> ②**击穿**（热点 key 过期）→ Redisson 分布式锁 + 双重检查  
> ③**雪崩**（大量 key 同时过期）→ 随机 TTL 抖动  
> ④**一致性**（写后缓存与 DB 不一致）→ 延迟双删 + Kafka 广播失效

### Q6："Kafka 消息丢了怎么办？"

> Producer 端 `acks=all` + `enable.idempotence=true` 保不丢。兜底：①事件日志表记录了所有需处理的事件 ②定时扫描 DB 订单表 `status='PENDING'` 但长时间未处理的记录 → 补偿触发处理。这是"事件溯源 + 定时补偿"的双保险。

### Q7："Lua 脚本在 Redis Cluster 下有问题吗？"

> Lua 脚本操作的 key 必须在同一个 slot。当前方案只有 `stock:product:{id}:amount` 一个 key，天然符合。跨 key 操作需用 hash tag：`stock:product:{groupId}:{id}:amount`（`{}` 内是 hash 计算依据）。

### Q8："布隆过滤器误判率和容量怎么算？"

> 误判率 p ≈ (1 - e^(-kn/m))^k。k=8, m≈5.36亿, n≈1万 → p ≈ 2.7×10⁻⁵（几乎无假阳性）。商品数增长时可接受稍高误判率（穿透后有空值缓存兜底）。容量上限约 3000 万商品（那时位数组可扩展到 1GB）。

### Q9："50 万 QPS 的秒杀场景，你的方案能撑住吗？"

> 不能。当前方案瓶颈：单机 Redis 约 10w QPS，DB 单行约 1000 QPS。50w QPS 需要：Redis Cluster 多分片 + Nginx/Lua 层前置限流 + **库存分桶**（一个商品库存拆成 100 个 key，竞争分散 100 倍）+ DB 异步批量落库。**坦白说这是大厂中间件团队做的事情，我的项目验证的是核心思想的正确性。**

### Q10："你对项目中哪个技术决策最不满意？要重做会怎么改？"

> ①**订单状态用 String 而非 Enum** → 改用 `@Enumerated(EnumType.STRING)`。  
> ②**JWT 用用户密码做密钥** → 改用配置中心的统一密钥。  
> ③**超时取消是被动触发** → 加 `@Scheduled` 主动扫描。  
> ④**布隆过滤器位数组硬编码** → 可配置化 + 支持运行时重建。  
> ⑤**Kafka 异步发送可能丢事件** → 改为先写事件日志表 + 定时轮询发送（Outbox 模式）。

---

## 9. 项目中的 Bug 与教训

| 版本 | 问题 | 修复 | 启示 |
|------|------|------|------|
| 乐观锁 | 高冲突导致库存无法扣光 | 换行锁 → 最终换 Lua | 乐观锁不适合高竞争写场景 |
| 行锁 | QPS 直接减半 | 引入 Redis Lua | 数据库行锁是单机单行瓶颈 |
| 布隆过滤器 | 逻辑反转：`mightContain=false` 跳过了正常请求 | 修正判断逻辑 | 单元测试应覆盖正反两个逻辑路径 |
| Session 存 Account | Session 膨胀，OOM 风险 | 只存 userId, timeout=15min | Tomcat Session 默认存内存，存大对象是坑 |
| InputStream 未关闭 | 文件上传未 try-with-resources | 全部加 try-with-resources + finally | Java IO 一定用 try-with-resources |
| Lettuce 连接池 | max-wait 默认无限 → OOM | 设置 max-wait=3000ms | 连接池参数必须调优，不能依赖默认值 |
| getAllProducts 无分页 | 全量数据加载到内存 | 添加分页 | 任何返回列表的接口都要考虑分页 |

---

## 10. 附录：快速定位代码指南

| 技术点 | 核心文件（shopping-guide-agent 分支） |
|--------|--------------------------------------|
| Redis Lua 扣减 | [StockRedisService.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/StockRedisService.java) |
| Lua 扣减脚本 | [deduct_stock.lua](tomatomall_backend/src/main/resources/lua/deduct_stock.lua) |
| Lua 回滚脚本 | [restore_stock.lua](tomatomall_backend/src/main/resources/lua/restore_stock.lua) |
| AOP 库存同步 | [StockRefreshAspect.java](tomatomall_backend/src/main/java/com/example/tomatomall/aspect/StockRefreshAspect.java) |
| 自定义注解 | [AutoRefreshStock.java](tomatomall_backend/src/main/java/com/example/tomatomall/annotation/AutoRefreshStock.java) |
| 下单扣库存 | [CartServiceImpl.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/CartServiceImpl.java) |
| 取消订单 | [OrdersServiceImpl.java](tomatomall_backend/src/main/java/com/example/tomatomall/service/serviceImpl/OrdersServiceImpl.java) |
| DB 行锁 SQL | [StockpileRepository.java](tomatomall_backend/src/main/java/com/example/tomatomall/Repository/StockpileRepository.java) |
| 多级缓存工具 | [RedisCacheUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/RedisCacheUtil.java) |
| Caffeine 配置 | [CaffeineConfig.java](tomatomall_backend/src/main/java/com/example/tomatomall/configure/CaffeineConfig.java) |
| 布隆过滤器 | [BloomFilterUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/BloomFilterUtil.java) |
| 缓存失效通知 | [CacheInvalidationListener.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/CacheInvalidationListener.java) |
| Kafka 配置 | [KafkaConfig.java](tomatomall_backend/src/main/java/com/example/tomatomall/configure/KafkaConfig.java) |
| Kafka 消费者 | [OrderCreatedConsumer.java](tomatomall_backend/src/main/java/com/example/tomatomall/kafka/OrderCreatedConsumer.java) |
| 事件日志实体 | [KafkaEventLog.java](tomatomall_backend/src/main/java/com/example/tomatomall/po/KafkaEventLog.java) |
| 支付验签 | [AlipayUtils.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/AlipayUtils.java) |
| JWT 认证 | [AuthUtil.java](tomatomall_backend/src/main/java/com/example/tomatomall/utils/AuthUtil.java) |
| AOP 监控 | [ApiCallStatisticsAspect.java](tomatomall_backend/src/main/java/com/example/tomatomall/aspect/ApiCallStatisticsAspect.java) |
| Redisson 配置 | [RedissonConfig.java](tomatomall_backend/src/main/java/com/example/tomatomall/configure/RedissonConfig.java) |
| Docker 部署 | [docker-compose.yml](docker-compose.yml) |
| Agent 入口 | [shopping_agent.py](tomatomall_agent/src/agent/shopping_agent.py) |
| Agent 工具集 | [product_tool.py](tomatomall_agent/src/tool/product_tool.py) |
| FastAPI 服务 | [app.py](tomatomall_agent/src/app.py) |
| Agent 记忆 | [chat_memory.py](tomatomall_agent/src/memory/chat_memory.py) |
| Agent 使用指南 | [AGENT_GUIDE.md](tomatomall_agent/AGENT_GUIDE.md) |

---

> **使用建议**：
> 1. 面试前通读一遍，每个追问准备 2-3 分钟口述版本
> 2. **第 1-3 节是核心**——库存扣减演进、缓存架构、Kafka 可靠消费是面试官最可能在简历上标注追问的方向
> 3. **Git commit 是你的王牌**——解释技术选型时用"一开始...#→#→#"的叙事比"我们选了 Redis Lua"有说服力 10 倍
> 4. 打开 IDE，跟着附录中的代码路径走一遍，确保能准确定位到关键代码
> 5. 面试时主动展示你修的 Bug（布隆过滤器反转、Session OOM、连接池超时），这比功能开发更体现工程能力
